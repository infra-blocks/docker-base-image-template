services:
  base:
    image: public.ecr.aws/infra-blocks/docker-base-image-template:latest
    build:
      context: ../
      dockerfile: docker/base/Dockerfile
    container_name: docker-base-image-template
